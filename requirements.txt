# Cryptography — AES-256-GCM encryption
cryptography>=42.0.0

# Argon2id — memory-hard master-password key derivation
argon2-cffi>=23.1.0

# Two-factor authentication — TOTP code generation and verification
pyotp>=2.9.0

# QR code generation — display 2FA setup codes to users
qrcode[pil]>=7.4.0

# Image handling — required by qrcode[pil] and used in the UI
Pillow>=10.0.0

# File system monitoring — detect external changes to database files on macOS
watchdog>=3.0.0
